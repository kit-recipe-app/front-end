import 'package:flutter/material.dart';

// Hier werden die TextStyles für die verschiedenen Texte eingefügt, die dann verwendet werden
class FontStyles {
  // Texte in den AppBars
  static TextStyle appBarText = const TextStyle(
    fontSize: 25,
    letterSpacing: 0,
    color: Colors.white,
  );

}

