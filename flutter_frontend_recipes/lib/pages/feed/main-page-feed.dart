import 'package:flutter/material.dart';

class MainPageFeed extends StatelessWidget {
  const MainPageFeed({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("feed screen"),
    );
  }
}
